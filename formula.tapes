let A : sort.   let B : sort.   let C : sort.

let t1 : term = s+(U, V) * s+(U1, V1).

check t1.
draw t1 to "./result_examples/times_of_swapsum.txt".

let t2 : tape = [gen(f, A, B) * gen(f, A, B)].

let t4 : term = gen(f, A, B).

draw t2 to "./result_examples/generators.txt"

let t3 : term =  copy