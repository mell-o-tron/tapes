let A : sort.   let B : sort.   let C : sort.
let f : A -> B * C.

set (otimesdist : 1.0). // for now these have to be float
set (oplusdist : 1.0).

let t1 : term = copy(A + B + C).

check t1.

draw t1 to "./result_examples/distributor"
